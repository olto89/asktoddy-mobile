#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged for automatic formatting and linting
npx lint-staged

# Type-check (disabled: pre-existing errors in scripts/supabase files)
# npm run type-check

# Run tests on changed files (fast, bail on first failure)
npx jest --onlyChanged --passWithNoTests --bail

# Save context before commit
npm run context:save
