#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged for automatic formatting and linting
npx lint-staged

# Temporarily disable TypeScript check for character integration
# npm run type-check

# Save context before commit
npm run context:save
